- name: demo on error handling
  hosts: local
  connection: local
  become: yes
  tasks: 
   - name: check roboshop user exists or not
     command: id roboshop
     register: user_output

   - name: print user out_put
     debug:
       msg: "{{ user_output }}"
     
