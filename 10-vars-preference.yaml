- name: variables preference
  hosts: frontend
  vars:
     full_name: "siva from play level"
  vars_files:
   - course.yaml
  vars_prompt:
   - name: full_name
     prompt: "please enter your full_name"
     private: false
  tasks:
  - name: print vars preference
    vars:
     # full_name: "siva from Task Level"
    debug:
      msg: "Hi, {{ full_name }}"


      # Highest Preferences of execution

      # 1. CMD args
      # 2. Task level