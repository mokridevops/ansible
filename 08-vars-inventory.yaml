- name: demo on inventory variables
  hosts: localhost
  connection: local # it does not need user id and password
  tasks:
   - name: print information
     debug:
      msg: "Hi, I am studying {{COURSE}}, Trainer is {{TRAINER}}, Duration is {{DURATION}}"