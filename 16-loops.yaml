- name: install packages
  hosts: local
  connection: local
  become: yes
  tasks:
   - name: install packages
     package:
       name: "{{ item }}"
       state: present
     loop:
     - nginx
     - mysql
     - mongodb-mongosh
     - git